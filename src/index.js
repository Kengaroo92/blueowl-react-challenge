import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return (
        <div className="temporary">Hello There!</div>
    );
};

ReactDOM.render(<App />, document.querySelector('#root'));